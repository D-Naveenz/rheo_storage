# rheo_tool

`rheo_tool` is the unified operator CLI for this repository.

It combines:

- repository configuration and version management
- local CI-style verification
- multi-runtime NuGet packaging and publish flows
- definitions package building and inspection commands
- an interactive shell for common maintenance tasks

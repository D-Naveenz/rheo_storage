# Rheo.Storage for .NET

`Rheo.Storage` is the managed .NET wrapper over the native `rheo_storage_ffi`
runtime.

It targets desktop .NET on Windows and packages the native Rust DLL alongside
the managed API.
